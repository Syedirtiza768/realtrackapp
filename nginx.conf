# /etc/nginx/sites-available/realtrackapp
# Optimized for performance: gzip, static caching, proxy buffering

server {
    listen 80;
    server_name mhn.realtrackapp.com;

    client_max_body_size 200m;
    client_body_timeout 3600s;

    # ── Gzip compression ─────────────────────────────────────
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_min_length 256;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml
        application/rss+xml
        image/svg+xml
        font/woff2;

    # ── Static frontend (Vite build output) ──────────────────
    root /var/www/html/realtrackapp/dist;
    index index.html;

    # Hashed assets: cache forever (Vite adds content hashes)
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Favicon / static files at root
    location ~* \.(ico|png|svg|jpg|jpeg|gif|webp|woff2?|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public";
        access_log off;
        try_files $uri =404;
    }

    # Large catalog CSV multipart (avoid 504 from short proxy_send_timeout)
    location ~* ^/api/catalog-import/upload$ {
        proxy_pass http://127.0.0.1:4191;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";

        client_max_body_size 200m;
        client_body_timeout 3600s;

        proxy_request_buffering off;
        proxy_buffering off;

        proxy_connect_timeout 30s;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;

        add_header Cache-Control "no-store";
    }

    # ── Image proxy (S3 streaming) ───────────────────────────
    # Cache proxied S3 images in nginx to avoid hitting Node for every request
    proxy_cache_path /tmp/nginx-image-cache levels=1:2 keys_zone=image_cache:10m max_size=1g inactive=30d use_temp_path=off;

    location /api/storage/serve/ {
        proxy_pass http://127.0.0.1:4191;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";

        proxy_buffering on;
        proxy_buffer_size 64k;
        proxy_buffers 16 64k;
        proxy_busy_buffers_size 128k;

        proxy_connect_timeout 10s;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;

        proxy_cache image_cache;
        proxy_cache_valid 200 30d;
        proxy_cache_valid 404 1m;
        proxy_cache_key $uri;
        proxy_cache_use_stale error timeout updating;
        add_header X-Cache-Status $upstream_cache_status;

        add_header Cache-Control "public, max-age=31536000, immutable";
        add_header Access-Control-Allow-Origin "*";
        access_log off;
    }

    # ── API reverse proxy ────────────────────────────────────
    location /api/ {
        proxy_pass http://127.0.0.1:4191;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Keep-alive to backend
        proxy_set_header Connection "";

        # Proxy buffering for faster response to client
        proxy_buffering on;
        proxy_buffer_size 16k;
        proxy_buffers 8 32k;
        proxy_busy_buffers_size 64k;

        # Timeouts (catalog upload uses dedicated location above)
        proxy_connect_timeout 15s;
        proxy_read_timeout 300s;
        proxy_send_timeout 600s;

        # Don't cache API responses in Nginx (backend handles caching)
        add_header Cache-Control "no-store";
    }

    # ── SPA fallback ─────────────────────────────────────────
    location / {
        try_files $uri $uri/ /index.html;

        # index.html should never be cached (contains asset hashes)
        location = /index.html {
            add_header Cache-Control "no-cache, no-store, must-revalidate";
            add_header Pragma "no-cache";
            add_header Expires "0";
        }
    }
}
